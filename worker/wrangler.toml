name = "satellite-imagery-proxy"
main = "src/index.js"
compatibility_date = "2026-04-01"

# Replace the placeholder id below with the value printed by:
#   wrangler kv namespace create QUOTA
kv_namespaces = [
  { binding = "QUOTA", id = "REPLACE_WITH_NEW_KV_NAMESPACE_ID" }
]

# MAPBOX_TOKEN is set as a secret, not in this file:
#   wrangler secret put MAPBOX_TOKEN
